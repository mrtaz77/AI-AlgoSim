#pragma once
#include <string>
using namespace std;

bool is_valid_grid_size(int);
bool is_valid_block_number(int, int);
int convert_block_label_to_number(string, int);
int validate_block_label(string, int);